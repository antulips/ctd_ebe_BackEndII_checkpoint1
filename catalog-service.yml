server:
  port: 8081

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/finalBack

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
